<UserControl x:Class="Kiosk.control.ItemDessertInCart"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
		     Background="{x:Null}"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             mc:Ignorable="d" 
             Height="35">

    <Grid  Height="35">
            
        <Grid Margin="4" HorizontalAlignment="Right">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>


            <Button Grid.Column="0" Height="50" Width="60" Background="{x:Null}" BorderBrush="{x:Null}" VerticalAlignment="Center" HorizontalAlignment="Left">
                <materialDesign:PackIcon HorizontalAlignment="Left" Height="30" Width="30" Kind="Close" Foreground="#424242" VerticalAlignment="Center" />
            </Button>

            <TextBlock Grid.Column="1" Text="پیاز      " FontSize="18" Foreground="#616161" VerticalAlignment="Center" HorizontalAlignment="Right" TextAlignment="Right" FlowDirection="RightToLeft" FontFamily="Vazir"/>

        </Grid>
    </Grid>
</UserControl>
